#!/bin/bash
set -e

SCRIPT_HOME="$( cd "$( dirname "$0" )" && pwd )"
CHARMANDER_HOME=$(pwd)

cd $CHARMANDER_HOME

for LAB_NODE in $(cat node_lab.txt)
do
    vagrant ssh -c "sudo /vagrant/bin/invm/build_experiments.sh maxusage" $LAB_NODE
done

cd - > /dev/null
